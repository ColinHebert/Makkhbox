:hierarchy:
    - '%{::env}/%{calling_module}'
    - '%{::env}/common'
    - '%{calling_module}'
    - 'common'

:backends:
    - gpg
    - yaml
    - file

:gpg:
    :datadir:
        hieradata

:yaml:
    :datadir:
        hieradata

:file:
    :interpolate:
        false
    :datadir:
        hieradata
